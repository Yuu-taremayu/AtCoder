#include <bits/stdc++.h>
using namespace std;

int main(void)
{
	long int A, B, N;

	cin >> A >> B >> N;

	int max_num = floor(A * (B - 1) / B);
	int times = N / B;

	for (int i = 0; i < times; i++) {
		
	}

	cout << max_num << endl;
	return 0;
}
